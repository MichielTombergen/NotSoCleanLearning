@model IEnumerable<CleanLearning.Models.TestResultFeedback>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReadStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TestResult.ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReadStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TestResult.ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
